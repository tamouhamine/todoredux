export default class{
    id
    title
    completed

    constructor(id=1,title='no title',completed=false){
        this.id = id
        this.title = title
        this.completed = completed
    }
}